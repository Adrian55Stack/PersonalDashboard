version: "3.8"

services:
  db:
    image: mysql:8
    container_name: personal_dashboard_database
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: personal-dashboard
    ports:
      - "3307:3306"
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data: